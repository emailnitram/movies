<div class="page-header">
  <h1>All Genres</h1>
</div>
<div class="row well">
	<div class="genre">
	  <% @genres.each do |g| %>
	    <div>
	      <p><%= link_to g.name, g %></p>
        </div>
  	  <% end %>
	</div>
</div>

<div>
  <%= link_to "Movies", movies_path, class: "btn btn-warning" %>
</div>