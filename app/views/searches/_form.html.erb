<%= simple_form_for(@searches) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.hidden :search,  %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
